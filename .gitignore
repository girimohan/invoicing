# dependencies
node_modules/

# next.js
.next/
out/

# env files
.env
.env.local
.env*.local

# SQLite database (contains personal data — do not commit)
prisma/dev.db
prisma/dev.db-journal
# seed.db is the schema-only template (no user data) — intentionally committed

# Electron build output (large binaries — distribute separately)
dist-electron/

# misc
.DS_Store
*.pem
npm-debug.log*
